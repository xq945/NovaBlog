<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novablog.chat.mapper.ChatSessionMapper">

    <resultMap id="sessionResultMap" type="com.novablog.chat.entity.ChatSession">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="messageCount" column="message_count"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <resultMap id="sessionVOResultMap" type="com.novablog.chat.vo.ChatSessionVO">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="messageCount" column="message_count"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_session (user_id, title, message_count)
        VALUES (#{userId}, #{title}, #{messageCount})
    </insert>

    <select id="findById" resultMap="sessionResultMap">
        SELECT * FROM chat_session WHERE id = #{id} LIMIT 1
    </select>

    <select id="findByUserId" resultMap="sessionVOResultMap">
        SELECT id, title, message_count, create_time, update_time
        FROM chat_session
        WHERE user_id = #{userId}
        ORDER BY update_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM chat_session WHERE user_id = #{userId}
    </select>

    <update id="updateTitle">
        UPDATE chat_session SET title = #{title} WHERE id = #{id}
    </update>

    <update id="incrementMessageCount">
        UPDATE chat_session SET message_count = message_count + #{delta} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM chat_session WHERE id = #{id}
    </delete>

</mapper>
